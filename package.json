{
  "name": "cid-utils-website",
  "version": "1.0.0",
  "description": "Website for decoding CIDs",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist/*",
    "build": "parcel build index.html --no-minify --public-url '.'",
    "dev": "parcel index.html",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "ipfs add -r -q dist | tail -n1 >versions/current; cat versions/current >> versions/history; cat versions/current"
  },
  "keywords": [],
  "author": "Victor Bjelkholm",
  "license": "MIT",
  "dependencies": {
    "cids": "^0.5.2",
    "ipfs-css": "^0.2.0",
    "multibase": "^0.4.0",
    "multicodec": "^0.2.6",
    "multihashes": "^0.4.13",
    "parcel-bundler": "^1.6.2",
    "tachyons": "^4.9.1",
    "varint": "^5.0.0"
  }
}
